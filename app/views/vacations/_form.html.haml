= simple_form_for @vacation, remote: true do |f|
  = f.input :description, as: :text, label: false, placeholder: "Enter description"
  = f.input :start_date
  = f.input :end_date
  = f.input :days
  = f.submit "Create", class: "btn btn-lg btn-default pull-left"
