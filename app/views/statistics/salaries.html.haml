- all_salaries = @salaries.sum("price")
- all_payments = @payments.sum("price")
- all_expenses = @expenses.sum("price")
- all_taxes = @taxes.sum("price")
- all_dohodnina = @dohodnina.sum("price")

= "All salaries #{all_salaries}"
%br
= "All payments #{all_payments}"
%br
= "All expenses #{all_expenses} -> Taxes #{all_taxes} + Dohodnina #{all_dohodnina}"
%br
= "Balance #{all_payments - all_expenses}"
%br

- @user_salaries.keys.each do |key|
  %b= key
  = "Earned: #{@user_salaries[key]} to get: #{(@max_salary - @user_salaries[key])}"
  %br
